#!/bin/sh -eu

cd "$(dirname "$0")/.."

nix run \
    --ignore-environment \
    --keep LANG \
    --keep LOCALE_ARCHIVE \
    --file support \
    nix-project-org2gfm \
    pkgs.ansifilter \
    pkgs.coreutils \
    pkgs.git \
    pkgs.gnugrep \
    pkgs.gnutar \
    pkgs.gzip \
    pkgs.tree \
    --command \
    org2gfm \
    --ignore links.org \
    --nix "$(command -v nix)" \
    --evaluate \
    "$@"
